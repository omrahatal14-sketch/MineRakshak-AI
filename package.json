{
  "name": "minerakshak-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-based Smart Governance & Statutory Compliance Platform for Coal Mines",
  "scripts": {
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:ai": "uvicorn app.main:app --reload --port 8000 --app-dir ai-service",
    "dev": "concurrently -n \"BACKEND,FRONTEND,AI-SERVICE\" -c \"cyan,magenta,green\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"uvicorn app.main:app --reload --port 8000 --app-dir ai-service\"",
    "seed": "npm run seed --prefix backend",
    "test": "npm test --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
